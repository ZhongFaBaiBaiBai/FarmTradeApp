<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="农产品买卖记录"
        android:theme="@style/Theme.FarmTrade"
        android:supportsRtl="true">

        <activity
            android:name=".ui.RecordListActivity"
            android:exported="true"
            android:label="买卖记录">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.AddRecordActivity" android:label="新增记录" />
        <activity android:name=".ui.QuickRecordActivity" android:label="快速记录" />
        <activity android:name=".ui.ChartActivity" android:label="数据图表" />
        <activity android:name=".ui.SummaryActivity" android:label="汇总统计" />
        <activity android:name=".ui.SettingsActivity" android:label="设置" />

        <!-- FileProvider：用于拍照（ActivityResultContracts.TakePicture）共享临时图片 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
